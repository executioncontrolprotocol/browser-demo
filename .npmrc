save-workspace-protocol=rolling
# Match pnpm-lock.yaml settings.autoInstallPeers. Optional vendor peers
# (fal, image-sharp, jsonata, …) are linked via link:vendor, not auto-installed.
auto-install-peers=false
